# coding: utf-8
# 把典型的代码流或数据流用lambda表达式的思想简化他们: 先把所有的相关方法用lambda来表示，oo系统只是用lambda名来表示
describe.(one_class) do
  before.(:each) do
  end
  after.(:each) do
  end
  should.("describe something") do
    #obj.should.(do_something) 
  end
  it.("describe something") do
  end
end
